mutation SendMessage($init:MessageInit!) {
  sendMessage(message: $init) {
    id
    author {
      id
      tag
      displayName
    }
    createdAt
    content
    recipient {
      asUser {
        id
        tag
        displayName
      }
    }
  }
}
