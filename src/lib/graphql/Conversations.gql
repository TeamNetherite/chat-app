query Conversations @cache(policy: CacheAndNetwork) {
  conversations {
    recipient {
      asUser {
        id
        tag
        displayName
        status
      }
      asChannel {
        id
        name
      }
      kind
    }
  }
}
